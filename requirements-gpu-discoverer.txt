# Discoverer's GB200/B200 nodes use the newer CUDA 13 user-space stack.
# Keep this separate from requirements-gpu.txt, which deliberately targets
# Leonardo's CUDA 12 A100 environment.
-r requirements.txt
# [ctk] brings the CUDA maths libraries (cuBLAS, cuFFT, ...): the venv has no
# system CUDA toolkit, and a matrix product fails with "libcublas.so.13" without it.
cupy-cuda13x[ctk]>=13,<15
