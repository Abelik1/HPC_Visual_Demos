.venv/
__pycache__/
*.pyc
runs/*
!runs/.gitkeep
.DS_Store
config/leonardo.env
# smallstep CA config and SSH certificates/keys for CINECA access - never commit
.step/
benchmarks/artifacts/
tmp/

# Gaia DR3 sky for the black-hole demo (35 MB): python tools/fetch_gaia_sky.py
data/gaia_sky.npz

# Saved-run bundles (tools/export_runs.py); carried by Drive/USB, never committed
exports/
/leonardo_runs_*.zip

# Demo-day videos (downloaded from Google Drive); only the README is versioned
videos/*
!videos/README.md

# Cluster settings edited in the dashboard (Leonardo account, certificate e-mail)
config/clusters.local.json

# Local profiling and campaign logs
benchmarks/*.log
